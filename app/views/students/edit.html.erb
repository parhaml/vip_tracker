<h1>Edit Student</h1>


<%= form_for @student do |f| %>
  <%= hidden_field_tag :id, :value => @student.id %>
	<%= f.label :name %>:
	<%= f.text_field :name %>

	<%= f.label :birthday %>
	<%= f.date_field :birthday %>

	<%= f.submit %>
<% end %>